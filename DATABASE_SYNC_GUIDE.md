# 数据库同步功能使用指南

## 🎯 功能概述

本系统实现了数据库表 `user_profiles` 与 Supabase Auth 用户的完全同步功能，确保用户认证数据与用户档案数据始终保持一致。

## 🔧 技术架构

### 核心组件

1. **数据库触发器** - 实时自动同步
2. **同步监控API** - 提供同步状态查询
3. **前端监控界面** - 可视化同步状态
4. **强制同步功能** - 手动修复数据不一致

### 同步机制

#### 自动同步（数据库触发器）
- **新用户注册**：自动创建用户档案
- **用户信息更新**：自动更新档案信息
- **用户登录**：自动更新最后登录时间

#### 手动同步
- **强制同步**：管理员可手动触发全量同步
- **状态监控**：实时查看同步状态和统计

## 📋 部署步骤

### 1. 应用数据库迁移

运行以下 SQL 迁移文件来创建同步机制：

```bash
# 在 Supabase SQL Editor 中执行
\i supabase/migrations/user_profiles_sync.sql
```

### 2. 验证部署结果

迁移完成后，检查以下内容：

- ✅ `user_profiles` 表结构是否正确
- ✅ 触发器函数是否创建成功
- ✅ 同步状态视图是否可用
- ✅ 强制同步函数是否正常工作

### 3. 配置环境变量

确保以下环境变量已正确配置：

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

## 🚀 使用方式

### 访问同步监控

1. 以管理员身份登录系统
2. 进入后台管理页面
3. 点击 "数据同步" 标签页
4. 查看同步状态和统计信息

### 同步状态说明

#### 状态类型
- ✅ **已同步** - 数据完全一致
- ❌ **缺失档案** - Auth 用户缺少对应的档案记录
- ⚠️ **邮箱不一致** - 认证邮箱与档案邮箱不匹配
- ⚠️ **档案过期** - 档案更新时间晚于认证更新时间
- ⚠️ **登录时间不同步** - 最后登录时间不一致

#### 监控指标
- **总用户数** - 系统认证用户总数
- **已同步用户数** - 数据完全一致的用户数量
- **同步百分比** - 整体同步完成度
- **各类问题用户数** - 按问题类型统计的用户数量

### 强制同步操作

当发现数据不一致时，管理员可以：

1. 点击 "强制同步" 按钮
2. 系统将触发全量数据同步
3. 同步完成后查看最新的同步状态

## 🛠️ 故障排除

### 常见问题

#### Q1: 同步状态显示 "缺失档案"
**原因**：用户注册时触发器未正常工作
**解决**：
1. 检查触发器是否正常创建
2. 运行强制同步修复缺失档案
3. 验证新用户注册是否正常触发

#### Q2: 邮箱显示不一致
**原因**：用户在 Auth 中修改了邮箱但档案未更新
**解决**：
1. 检查 `handle_user_update` 触发器
2. 运行强制同步更新邮箱信息
3. 验证邮箱修改是否正常触发

#### Q3: 同步百分比低于100%
**原因**：存在历史数据不一致或触发器失效
**解决**：
1. 运行强制同步修复所有问题
2. 检查触发器函数是否正常运行
3. 监控新用户注册是否正常同步

### 日志检查

查看同步操作的详细日志：

```sql
-- 检查触发器执行日志
SELECT * FROM auth.users WHERE created_at > NOW() - INTERVAL '1 day';

-- 检查同步状态
SELECT * FROM public.user_sync_status WHERE sync_status != 'synced';

-- 检查同步统计
SELECT * FROM public.user_sync_stats;
```

## 📊 性能监控

### 同步性能指标
- **触发器响应时间**：通常 < 100ms
- **强制同步时间**：取决于用户数量（约 1秒/100用户）
- **监控刷新频率**：每30秒自动刷新

### 资源使用
- **数据库负载**：触发器操作对性能影响极小
- **网络开销**：监控API调用频率可控
- **存储需求**：同步状态视图占用少量存储空间

## 🔒 安全考虑

### 权限控制
- 只有管理员可以访问同步监控
- 强制同步需要管理员权限
- 数据库触发器使用 SECURITY DEFINER

### 数据保护
- 用户敏感信息已脱敏处理
- 同步操作记录完整审计日志
- 错误处理机制防止数据损坏

## 📈 扩展建议

### 未来增强功能
1. **批量同步操作** - 支持按条件批量同步
2. **同步历史记录** - 记录每次同步操作的详情
3. **邮件通知** - 同步失败时自动通知管理员
4. **性能优化** - 支持大用户量的高效同步

### 监控改进
1. **实时报警** - 同步失败时即时通知
2. **趋势分析** - 同步状态的历史趋势图表
3. **自动化修复** - 检测到问题时自动触发修复

## 🆘 技术支持

如果遇到技术问题，请检查：

1. **数据库连接** - 确认 Supabase 连接正常
2. **权限设置** - 验证管理员权限配置
3. **触发器状态** - 检查数据库触发器是否启用
4. **日志信息** - 查看控制台和数据库日志

如需进一步帮助，请联系系统管理员或参考 Supabase 官方文档。