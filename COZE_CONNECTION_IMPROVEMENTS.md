# Coze 连接改进说明

## 改进内容

为争议解决中心和牛马测评仪添加了以下改进：

### 1. 连接状态指示器

在对话界面顶部添加了实时连接状态显示：
- 🟢 绿色 - 已连接
- 🟡 黄色 - 连接中（脉冲动画）
- 🔴 红色 - 未连接

### 2. 重连功能

- 当连接断开时，显示"重新连接"按钮
- 最多允许重连 3 次
- 每次重连会重置消息并重新获取开场白

### 3. 防止重复初始化

- 使用 `useRef` 防止组件重新渲染时重复初始化
- 避免不必要的 API 调用

### 4. 改进的状态管理

添加了新的状态变量：
- `connectionStatus`: 连接状态 ('connecting' | 'connected' | 'disconnected')
- `retryCount`: 重连次数计数
- `initializationRef`: 防止重复初始化的引用

### 5. 错误处理增强

- 在每个 API 调用时更新连接状态
- 用户可以清楚地看到连接失败的原因
- 提供明确的错误提示

## 使用说明

### 正常使用

1. 页面加载时自动连接到 Coze API
2. 顶部显示连接状态
3. 连接成功后可以正常对话

### 连接失败

1. 如果连接失败，状态指示器显示红色"未连接"
2. 点击"重新连接"按钮尝试重连
3. 如果重连 3 次仍未成功，提示用户稍后再试

### 刷新页面

1. 刷新页面后，组件会自动重新初始化
2. 重新获取开场白并连接 Coze API
3. 不需要手动重连

## 技术细节

### 争议解决中心 vs 牛马测评仪

两个功能使用完全独立的 Coze 配置：

| 配置项 | 争议解决中心 | 牛马测评仪 |
|--------|-------------|------------|
| API 路由 | `/api/coze/chat` | `/api/spark-evaluator/chat` |
| API URL | `x6yjs8hc3k.coze.site` | `qz6hgwr9c2.coze.site` |
| Project ID | `7589147310182039571` | `7589925531894808617` |
| Token | 从环境变量读取 | 从环境变量读取（更新） |

**结论**: 不会有冲突，两个功能完全独立。

## 环境变量配置

### 本地开发 (.env.local)

```
# 争议解决中心
COZE_API_URL=https://x6yjs8hc3k.coze.site/stream_run
COZE_API_TOKEN=<你的Token>
COZE_PROJECT_ID=7589147310182039571

# 牛马测评仪（新增）
NIUMA_COZE_API_URL=https://qz6hgwr9c2.coze.site/stream_run
NIUMA_COZE_API_TOKEN=<你的Token>
NIUMA_COZE_PROJECT_ID=7589925531894808617
```

### Vercel 部署

需要在 Vercel Dashboard 的环境变量中添加上述所有变量。

## 部署检查清单

- [ ] `.env.local` 已配置所有环境变量
- [ ] `.env.production` 已配置所有环境变量
- [ ] `vercel.json` 已更新超时配置
- [ ] Vercel Dashboard 环境变量已设置
- [ ] 重新部署项目
- [ ] 测试争议解决中心功能
- [ ] 测试牛马测评仪功能
- [ ] 测试重连功能

## 已知问题

1. **刷新后不显示历史消息**：这是正常行为，每次刷新都会重新初始化对话
2. **Vercel 超时**：已将超时时间设置为 60 秒，如果仍超时需要优化 Coze 项目
3. **模拟响应**：当 API 失败时，系统会返回模拟响应作为降级方案

## 未来改进

- [ ] 添加消息持久化（保存到 localStorage 或数据库）
- [ ] 添加会话管理功能
- [ ] 优化 API 响应时间
- [ ] 添加更多快捷问题选项
