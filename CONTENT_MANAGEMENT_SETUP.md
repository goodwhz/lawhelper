# 后台内容管理功能设置指南

## 功能概述

后台内容管理功能为法律助手系统提供了完整的内容管理能力，包括：

### 主要功能
1. **法律文档管理** - 添加、编辑、删除法律文档，支持分类和标签
2. **分类管理** - 管理文档分类，支持层级分类结构
3. **对话记录查看** - 查看和管理用户对话记录
4. **高级搜索** - 支持全文搜索、分类过滤、标签过滤
5. **批量操作** - 批量删除、批量发布/取消发布、批量添加标签
6. **权限控制** - 基于角色的访问控制，只有管理员可访问

### 特色功能
- 🔍 **智能搜索** - 支持标题、内容、标签、用户等多维度搜索
- 🏷️ **标签系统** - 灵活的标签管理，便于内容分类和检索
- 📊 **统计信息** - 实时显示文档、分类、对话数量统计
- ✅ **批量操作** - 高效的批量管理功能
- 🔄 **实时更新** - 数据实时同步，无需刷新页面

## 安装步骤

### 1. 数据库设置

首先需要设置数据库表和策略：

#### 方法一：使用Supabase SQL编辑器
1. 登录您的 Supabase 控制台
2. 进入 SQL 编辑器
3. 复制 `scripts/setup-content-management.sql` 文件的内容
4. 粘贴并执行SQL脚本

#### 方法二：使用命令行
```bash
# 如果您有 Supabase CLI
supabase db push
```

### 2. 验证数据库结构

确保以下表已创建：
- `legal_documents` - 法律文档表
- `document_categories` - 文档分类表
- `conversations` - 对话表
- `messages` - 消息表

### 3. 权限配置

确保您的用户账户具有管理员权限：
1. 登录 Supabase 控制台
2. 进入 `user_profiles` 表
3. 确保您的用户 `role` 字段为 `'admin'`

如果没有管理员用户，可以手动设置：
```sql
UPDATE user_profiles 
SET role = 'admin' 
WHERE user_id = 'your-user-id';
```

### 4. 访问内容管理

1. 启动应用程序：`npm run dev`
2. 使用管理员账户登录
3. 访问 `/admin` 页面
4. 点击"内容管理"标签页

## 功能使用指南

### 文档管理

#### 添加文档
1. 点击"添加文档"按钮
2. 填写文档信息：
   - **标题**：文档标题（必填）
   - **内容**：文档正文内容（必填）
   - **分类**：选择合适的文档分类
   - **标签**：添加相关标签，用逗号分隔
   - **发布状态**：选择是否立即发布
3. 点击"保存"完成添加

#### 编辑文档
1. 在文档列表中找到要编辑的文档
2. 点击"编辑"按钮
3. 修改文档信息
4. 点击"保存"更新文档

#### 批量操作
1. 选择要操作的文档（勾选复选框）
2. 使用批量操作栏：
   - **批量添加标签**：为选中文档添加标签
   - **批量发布**：将选中文档设为发布状态
   - **批量取消发布**：将选中文档设为草稿状态
   - **批量删除**：删除选中文档

### 分类管理

#### 添加分类
1. 点击"添加分类"按钮
2. 填写分类信息：
   - **分类名称**：分类名称（必填）
   - **描述**：分类描述
   - **父分类**：可选择父分类创建子分类
3. 点击"保存"完成添加

#### 层级分类
- 支持多级分类结构
- 可以创建主分类和子分类
- 便于内容的组织和导航

### 对话记录管理

#### 查看对话
1. 在"对话记录"标签页中查看所有用户对话
2. 显示对话标题、用户信息、消息数量等
3. 点击"查看详情"可打开具体对话

#### 搜索对话
- 支持按对话标题搜索
- 支持按用户邮箱搜索
- 可按创建时间、更新时间排序

## 搜索和过滤功能

### 高级搜索
- **全文搜索**：在标题、内容中搜索关键词
- **分类过滤**：按文档分类筛选
- **标签过滤**：按标签筛选文档
- **用户过滤**：按用户筛选对话

### 排序选项
- **时间排序**：创建时间、更新时间（升序/降序）
- **标题排序**：按字母顺序排序
- **相关排序**：按相关性排序（对话）

## API接口

### 获取数据
```http
GET /api/admin/content?type=documents&page=1&limit=20&search=关键词
```

### 创建数据
```http
POST /api/admin/content
Content-Type: application/json

{
  "type": "documents",
  "action": "create",
  "data": {
    "title": "文档标题",
    "content": "文档内容",
    "category_id": "分类ID",
    "tags": ["标签1", "标签2"],
    "published": true
  }
}
```

### 批量操作
```http
POST /api/admin/content
Content-Type: application/json

{
  "type": "documents",
  "action": "batchUpdate",
  "data": {
    "ids": ["id1", "id2"],
    "updates": {
      "published": false
    }
  }
}
```

## 安全考虑

### 权限控制
- 所有操作都需要管理员权限验证
- 用户只能查看自己的对话（除管理员外）
- 文档的发布状态控制可见性

### 数据安全
- 使用 Supabase RLS (Row Level Security) 策略
- 前端验证和后端验证相结合
- 防止SQL注入和XSS攻击

## 故障排除

### 常见问题

#### 1. 权限不足错误
**问题**：显示"权限不足"或"未授权"
**解决方案**：
- 检查用户是否在 `user_profiles` 表中
- 确认用户 `role` 字段为 `'admin'`
- 重新登录以刷新权限

#### 2. 数据不显示
**问题**：列表为空或数据不完整
**解决方案**：
- 检查数据库表是否正确创建
- 确认 RLS 策略是否正确配置
- 检查 API 控制台是否有错误信息

#### 3. 搜索功能无效
**问题**：搜索无结果或搜索不准确
**解决方案**：
- 检查搜索关键词是否正确
- 确认数据库索引是否创建
- 检查搜索逻辑是否正确

#### 4. 批量操作失败
**问题**：批量操作无响应或失败
**解决方案**：
- 检查是否有选中项目
- 确认网络连接正常
- 查看浏览器控制台错误信息

### 调试模式

启用调试模式以获取更多信息：
```javascript
// 在浏览器控制台中
localStorage.setItem('debug', 'true')
```

## 扩展功能

### 计划功能
1. **内容审核** - 添加内容审核流程
2. **版本控制** - 文档版本管理
3. **导入导出** - 批量导入导出功能
4. **统计分析** - 访问统计、使用分析
5. **自动分类** - AI辅助文档分类

### 自定义开发
可以通过修改以下文件来扩展功能：
- `app/components/admin/ContentManagement.tsx` - 主要组件
- `app/api/admin/content/route.ts` - API接口
- `types/content.ts` - 类型定义

## 技术支持

如果您在设置或使用过程中遇到问题，可以：

1. 查看浏览器控制台错误信息
2. 检查 Supabase 日志
3. 验证网络请求响应
4. 参考 Next.js 和 Supabase 官方文档

---

**注意**：在开始使用之前，请确保已完成所有数据库设置和权限配置。建议先在测试环境中验证功能正常。